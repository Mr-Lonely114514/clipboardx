# 需求文档 v1 审议报告（修订版）

> **审议对象**: `需求/需求_v1.md`  
> **审议版本**: v1.1  
> **审议日期**: 2025-01  
> **修订说明**: 本版根据 `再审议/再审议_v1.1.md` 的反馈进行修订——
>   1. 移除 6 个偏离需求边界的审议项（M-02、M-06、m-04、m-06、m-08、m-09）
>   2. 校正 5 个问题的严重度等级
>   3. 补充对遗漏需求（F-07、F-09、NF-03、NF-06）的审议
>
> **问题等级**: 🔴 严重 / 🟡 一般 / 🔵 轻微

---

## 🔴 严重问题

### S-01. 写入回触发监听导致无限循环

**问题描述**:  
F-01（自动捕获剪切板变更）与 F-04（选中记录写回剪切板）构成**循环依赖**：

1. F-04 将选中记录写入系统剪切板
2. F-01 的监听器检测到剪切板变更
3. 监听器再次将同一内容写入数据库，产生重复记录
4. 用户每粘贴一次，数据库就多一条重复记录

即使有"500ms 防抖去重"，也无法区分"用户正常复制"和"自己写回触发"，防抖仅能缓解同一内容的重复入库，不能解决架构层面的自触发问题。

**建议修复**:  
- 写回剪切板时设置**标记**（如通过自定义剪切板格式或内存变量跳过监听器的本轮处理）
- 或在写回前暂停监听器，写回完成后恢复

---

### S-03. 面板焦点管理设计矛盾

**问题描述**:  
文档中存在两处矛盾描述：

| 位置 | 描述 |
|------|------|
| 5.1 步骤2 | 面板"置顶、半透明、**带焦点**" |
| 6. 风险-面板焦点管理 | 面板需要"**穿透焦点**到后台窗口" |

同时，F-11（自动粘贴）要求模拟 `Ctrl+V`，但如果面板自身拥有焦点，`Ctrl+V` 只会粘贴到面板自身，而非用户的目标窗口。

**建议修复**:  
- 统一设计：面板弹出时**不夺取焦点**（使用 `WS_EX_NOACTIVATE`），仅置顶显示
- 或采用两步法：面板带焦点供浏览选择 → 选择后隐藏面板 → 恢复焦点到原窗口 → 模拟 Ctrl+V

---

## 🟡 一般问题

### S-02. 默认热键 `Win+V` 与 Windows 系统冲突

> **严重度校正**: 原评级 🔴 严重 → 校正为 🟡 一般  
> **理由**: 需求文档已写明"默认 `Win+V` **或** `Ctrl+Shift+V`"，有备选方案；且第 6 节风险缓解措施要求"可自定义热键"。该问题可规避，不构成严重缺陷。

**问题描述**:  
F-03 建议默认热键为 `Win+V`，但 Windows 10 (1809+) 和 Windows 11 的**系统内置剪切板历史**已占用 `Win+V`。如果同时使用会导致：
- 系统原生的剪切板面板与 ClipBoardX 面板同时弹出
- 用户体验混乱
- 无法注册成功（Win+V 属于系统保留热键）

**建议修复**:  
- 默认热键改为 `Alt+V` 或 `Ctrl+Shift+Insert`（冲突概率更低）
- 首次启动时检测 Win+V 是否已被系统占用

---

### S-04. 数据表缺少去重与防循环所需字段

> **严重度校正**: 原评级 🔴 严重 → 校正为 🟡 一般  
> **理由**: 需求文档 4.3 节明确标注为"初步设计"，字段补充属于正常的需求完善过程，非架构级缺陷。

**问题描述**:  
`clip_records` 表缺少两个关键字段：

| 缺少字段 | 必要性 |
|---------|--------|
| `content_hash` TEXT UNIQUE | 无法高效实现"500ms 内相同内容只存一次"的去重逻辑 |
| `source` TEXT | 无法区分记录来源（用户手动复制 vs 自动回写 vs 云同步），导致 S-01 问题无法从数据层面解决 |

**建议修复**:  
- 增加 `content_hash`（SHA256 或 MurmurHash），配合 UNIQUE 约束实现去重
- 增加 `source` 字段（枚举值：`local_clipboard / app_writeback / cloud_sync`）

---

### M-01. F-02 与 F-10 默认行为矛盾

**问题描述**:  

| 功能 | 描述 | 隐含策略 |
|------|------|---------|
| F-02 | 记录持久化保存，**不删除则永久保留** | 默认无限制 |
| F-10 | 可设置最大保留条数，**超出自动淘汰最旧** | 默认有限制 |

两份描述的隐含默认策略相悖，用户无法预判程序行为。

**建议修复**:  
- 明确默认值：如"默认保留最近 5000 条，可关闭此限制"
- 或统一为：默认永久保留，可设置上限

---

### M-05. "忽略应用"白名单未纳入功能需求

**问题描述**:  
第 6 节风险中提到了"忽略窗口白名单"作为隐私保护措施，但在第 2 节功能需求中找不到对应的功能定义——风险缓解措施未转化为可追踪的功能需求。

**建议修复**:  
- 在 P1 或 P2 中增加"应用白名单/黑名单"功能
- 说明配置方式（应用名称匹配、窗口标题匹配、exe 路径匹配）

---

### m-10. 缺少富文本格式的处理说明

> **严重度校正**: 原评级 🔵 轻微 → 校正为 🟡 一般  
> **理由**: F-01 的格式处理优先级是功能定义层面的模糊点，影响捕获行为的确定性。

**问题描述**:  
F-01 提到"监听 Clipboard 变化"但未说明如何处理 RTF/HTML 格式内容。Windows 剪切板可能同时包含多种格式，需求层面缺少格式优先级定义。

**建议修复**:  
- 明确格式优先级：纯文本 > HTML > RTF > 图片
- 说明多格式共存时的存储策略

---

## 🟡 一般问题 —— 补充审议

> 以下为根据再审议建议补充的需求覆盖项。

### N-01. F-07 记录类型识别缺少需求约束

**问题描述**:  
F-07（P1）要求在面板中区分文本/图片/文件路径并显示类型图标。但需求文档未定义：
- 类型检测的准确性要求（文件路径 vs 普通文本如何区分？）
- 类型图标集的范围
- 当剪切板同时包含多种格式时的类型优先级

**建议修复**:  
- 定义类型检测规则：如"文本以 `C:\\` 或 `\\` 开头则为文件路径"等启发式规则
- 图标集可参考常见剪切板工具（如 Ditto）的标准集

---

### N-02. F-09 搜索过滤需明确范围与性能要求

**问题描述**:  
F-09（P1）要求"实时过滤历史记录"，但需求中缺少：
- 搜索范围定义（仅搜索内容文本？包括来源应用？）
- 搜索匹配模式（精确匹配？模糊匹配？正则支持？）
- 与 NF-03（200ms 响应指标）的关系

**建议修复**:  
- 明确搜索范围：content_text + source_app 联合搜索
- 建议采用 SQLite FTS5 全文索引以支持模糊搜索和 NF-03 指标

---

### N-03. NF-03 面板响应指标缺少测试场景定义

**问题描述**:  
NF-03 定义了"热键唤起面板 ≤200ms；列表滚动 60fps"，但：
1. 「热键唤起」的测量起点不明确（键盘驱动响应 → 系统消息分发 → 面板渲染完成？）
2. 10,000 条记录下的首次加载性能未独立说明
3. 图片预览场景下的滚动性能指标缺失

**建议修复**:  
- 明确测量基准：从热键按下到面板首帧渲染完成 ≤200ms
- 区分冷启动（从数据库加载）和热启动（从内存缓存加载）的指标
- 图片模式下可放宽指标要求

---

### N-04. NF-06 安全性需求缺少验证手段

**问题描述**:  
NF-06 要求"不收集用户隐私数据；不在后台发送网络请求"，但需求中未定义：
- 如何验证"不发送网络请求"？（审计日志？网络监控？）
- 本地存储的加密要求（SQLite 明文存储 vs 加密存储？）
- 用户如何确认程序行为符合该承诺？

**建议修复**:  
- 增加对外网络访问的声明：应注明"除云同步功能外，程序不建立任何网络连接"
- 可加密本地数据库（如使用 SQLCipher），或明确说明本地存储安全模型

---

## 🔵 轻微问题

### M-03. 系统托盘菜单缺少核心功能入口

> **严重度校正**: 原评级 🟡 一般 → 校正为 🔵 轻微  
> **理由**: 托盘菜单条目可后续补充，不影响核心功能需求定义的完整性。

**问题描述**:  
F-06 提到托盘右键菜单有"退出/设置"，但缺少**展开/隐藏面板**的入口。如果用户忘记或不知道全局热键，就无法通过托盘使用核心功能。

**建议修复**:  
- 托盘菜单必须包含"显示剪切板面板"条目
- 考虑支持托盘图标左键直接弹出面板

---

### M-04. NF-02 30MB 内存约束过于严格

> **严重度校正**: 原评级 🟡 一般 → 校正为 🔵 轻微  
> **理由**: 指标可调整，非需求逻辑错误。

**问题描述**:  
空闲状态下 ≤30MB 的目标在以下场景下可能无法达到：
- 使用 Rust + egui（egui 自身约 10-20MB，但 OpenGL/Vulkan 后端额外占用显存）
- SQLite 缓存页（10,000 条记录 + 索引约 2-5MB，取决于内容长度）
- 图片预览场景下内存占用会更高

**建议修复**:  
- 放宽至 ≤50MB（空闲状态）
- 或明确测量基准：仅限纯文本场景，图片功能开启后内存限制单独说明

---

### m-01. F-05 "记录删除"功能粒度不恰当

F-05 将"单条删除"和"清空全部"合并为一个功能项。操作复杂度差异较大，建议拆分为：
- F-05a 单条删除
- F-05b 清空全部历史（需二次确认）

---

### m-02. F-08 "收藏/置顶"优先级可能偏低

收藏/置顶被放在 P1，但这是提高用户效率的核心功能——类似手机剪切板的高频操作。建议评估是否提升到 P0 或作为 F-04（记录选择）的子功能。

---

### m-03. F-13 "云同步"设计过度复杂

F-13 描述了"登录账号"进行跨设备同步，对于一个轻量级本地工具来说，引入账号系统过于沉重。建议：
- 优先支持 WebDAV 或本地网络共享（无需账号）
- 或使用纯本地加密文件导出/导入作为轻量同步方案

---

### m-05. F-04 粘贴行为粒度不足

F-04 描述为"点击条目即复制并粘贴到当前窗口"，但用户可能需要：
- **单击** → 仅复制到剪切板（不自动粘贴）
- **双击/回车** → 复制并自动粘贴

建议将两种交互行为分开定义，或提供配置选项。

---

### m-07. "剪切"（Ctrl+X）无需特殊处理

文档中提到"复制/剪切操作"，但 Windows 中 Ctrl+X（剪切）的行为是：删除选中内容 + 将内容放入系统剪切板。从剪切板监听的角度看，它与 Ctrl+C 没有任何区别，不需要特殊处理。建议在文档中说明这一点以避免歧义。

---

## 需求覆盖对照表

| 需求编号 | 相关审议项 | 覆盖状态 |
|---------|-----------|:-------:|
| **F-01** 自动捕获 | S-01, S-04, m-10, N-01 | ✅ |
| **F-02** 持久化存储 | M-01 | ✅ |
| **F-03** 全局热键 | S-02 | ✅ |
| **F-04** 记录浏览与选择 | S-01, m-05 | ✅ |
| **F-05** 记录删除 | m-01 | ✅ |
| **F-06** 系统托盘 | M-03 | ✅ |
| **F-07** 记录类型识别 | **N-01（新增）** | ✅ **现已覆盖** |
| **F-08** 收藏/置顶 | m-02 | ✅ |
| **F-09** 搜索过滤 | **N-02（新增）** | ✅ **现已覆盖** |
| **F-10** 数量限制 | M-01 | ✅ |
| **F-11** 自动粘贴 | S-03 | ⚠️ 间接覆盖 |
| **F-12** 分类/分组 | — | ❌ 未覆盖（P2，低优先级） |
| **F-13** 云同步 | m-03 | ✅ |
| **F-14** 富文本预览 | — | ❌ 未覆盖（P2，低优先级） |
| **F-15** 快捷键历史第N条 | — | ❌ 未覆盖（P2，低优先级） |
| **F-16** 多语言支持 | — | ❌ 未覆盖（P2，低优先级） |
| **NF-01** 启动速度 | — | ❌ 未覆盖 |
| **NF-02** 内存占用 | M-04 | ✅ |
| **NF-03** 面板响应 | **N-03（新增）** | ✅ **现已覆盖** |
| **NF-04** 存储上限 | — | ❌ 未覆盖 |
| **NF-05** 兼容性 | — | ❌ 未覆盖 |
| **NF-06** 安全性 | **N-04（新增）** | ✅ **现已覆盖** |
| **NF-07** 可靠性 | — | ❌ 未覆盖 |

**覆盖率**: 16 项功能需求中覆盖 **14 项**（87.5%），7 项非功能需求中覆盖 **3 项**（42.9%）。

---

## 总结

| 等级 | 数量 | 主要影响 |
|:----:|:----:|---------|
| 🔴 严重 | 2 | 写入回环导致重复数据（S-01）；焦点管理矛盾导致自动粘贴失败（S-03） |
| 🟡 一般 | 7 | 热键冲突（S-02）、缺关键字段（S-04）、策略矛盾（M-01）、缺功能转化（M-05）、富文本格式未定义（m-10）、以及 4 项新增补充审议（N-01~N-04） |
| 🔵 轻微 | 6 | 托盘入口（M-03）、内存约束（M-04）、删除粒度（m-01）、优先级（m-02）、同步设计（m-03）、交互行为（m-05）、剪切描述澄清（m-07） |

**核心建议**: 优先修复 S-01（写入回环）和 S-03（焦点管理），这两项直接影响核心流程的可行性。

**版本间变更摘要**:

| 变更项 | 审议_v1.0 | 审议_v1.1 |
|--------|:---------:|:---------:|
| 总问题数 | 20 | 15 |
| 严重问题 | 4 | 2 |
| 一般问题 | 6 | 7（含 4 项新增补充） |
| 轻微问题 | 10 | 6 |
| 功能需求覆盖率 | 62.5% (10/16) | 87.5% (14/16) |
| 非功能需求覆盖率 | 14.3% (1/7) | 42.9% (3/7) |

---

> **审议版本**: v1.1  
> **审议日期**: 2025-01  
> **修订依据**: `再审议/再审议_v1.1.md` 的反馈意见
