# Windows 全局剪切板管理器 —— 需求文档 v4

> **项目代号**: ClipBoardX  
> **创建日期**: 2025-01  
> **版本**: v4.0  
> **修订说明**: 本版根据 `审议/审议_v3.1.md` 的审议意见修订，重点修复 4 项严重问题与 5 项一般问题，轻微问题暂不处理。主要变更：F-01 存储策略调整为"保留最高优先级两种格式"解决格式优先级与类型识别的设计矛盾；F-02 回归 v1 简洁描述；F-04 交互模式重命名为"自动粘贴模式/确认后粘贴模式"消除命名混淆；F-07 文件路径检测增加路径存在性验证；F-10 禁用限制值改为 -1 并补充性能预期；F-17 补充匹配策略与性能考量；删除 F-18 合并入 NF-06；NF-03/NF-04 术语统一为"性能保证上限/存储上限 ≥ 10,000"；5.1 流程图简化监听器生命周期描述。

---

## 1. 项目背景

### 1.1 现状痛点

| 对比项 | Windows 默认剪切板 | 手机输入法剪切板（如搜狗/百度/讯飞） |
|--------|-------------------|-----------------------------------|
| 历史记录 | ❌ 仅保存最近 1 条 | ✅ 保存全部记录（不删除则永久保留） |
| 记录浏览 | ❌ 不支持 | ✅ 弹出列表，可上下翻页浏览 |
| 按需选择 | ❌ 不支持 | ✅ 点击任意条目即可粘贴 |
| 驻留方式 | 系统级（无 UI） | 输入法面板内常驻 |
| 分类管理 | ❌ 不支持 | ✅ 支持收藏/标记/分类 |

用户在日常 Windows 办公中频繁进行复制粘贴操作，但默认剪切板只能保留最近一次复制的内容，一旦复制新内容，旧内容即被覆盖丢失，严重降低工作效率。

### 1.2 目标

在 Windows 操作系统上实现一套**类手机输入法剪切板**的桌面端剪切板管理器，满足以下核心诉求：

- 自动监听并保存所有复制/剪切操作的历史记录
- 提供全局快捷唤起的剪切板面板（类似手机输入法的弹出列表）
- 支持在历史记录中浏览、搜索、选中并粘贴
- 记录持久化保存（SQLite 本地存储，重启不丢失）
- 支持可配置的最大保留条数（默认 5000 条，可设为无限制）
- 轻量、无侵入、低资源占用

---

## 2. 功能需求

### 2.1 核心功能（P0 - 必须实现）

| 编号 | 功能 | 描述 | 优先级 |
|------|------|------|--------|
| F-01 | 剪切板历史记录自动捕获 | 监听系统 Clipboard 变化（通过 `WM_CLIPBOARDUPDATE`），自动保存每次复制/剪切的内容。需明确格式处理优先级：**纯文本 > HTML > RTF > 图片**；多格式共存时**保留最高优先级的两种格式（纯文本 + 图片）**，文本内容存入 `content_text`，图片数据存入 `content_blob`，确保图片预览功能（F-14）在不同格式共存场景下仍可正常使用。<br><br>**写回跳过机制**: 当程序自身将内容写回系统剪切板时，必须通过以下方式之一跳过本轮监听，防止无限循环：① 写回前暂停监听器，写回完成后再恢复；② 通过内存标记/自定义剪切板格式标识本轮写入为"自触发"，监听器检测到后跳过。使用内存标记时需注意异常安全：写回过程中若发生异常，应确保标记位在 finally/Defer 块中被正确清除，防止标记泄漏导致后续监听失效。<br><br>**监听器生命周期**: 暂停/恢复操作必须覆盖"将内容写入系统剪切板"这一原子操作的全过程。暂停时机为**写入前一刻**，恢复时机为**写入完成后立即**，确保中间无其他剪切板变更事件被误捕获。推荐使用内存标记（标志位）而非实际暂停监听器，以减少窗口竞争条件。 | P0 |
| F-02 | 历史记录持久化存储 | 所有记录保存到本地 SQLite 数据库，程序重启后不丢失。 | P0 |
| F-03 | 全局热键唤起面板 | 用户按自定义热键弹出剪切板面板。**默认热键改为 `Alt+V`**（避免与 Windows 系统内置 `Win+V` 冲突）。备选方案：`Ctrl+Shift+Insert`。首次启动时检测默认热键是否被系统或其他程序占用，若被占用则提示用户重新设置。 | P0 |
| F-04 | 记录浏览与选择 | 面板内按时间倒序展示记录列表。点击任意条目时的行为：① 将选中内容写回系统剪切板（写回时触发跳过机制，参见 F-01）；② 面板隐藏；③ 恢复焦点到原活动窗口；④ 模拟 `Ctrl+V` 自动粘贴。<br><br>**交互模式设置**：支持两种交互模式，默认模式为**自动粘贴模式**：<br>- **自动粘贴模式（默认）**：单击某条记录 → 复制到剪切板 + 自动隐藏面板 + 自动粘贴（即上述完整流程）<br>- **确认后粘贴模式**：单击仅复制到剪切板（不自动粘贴，不隐藏面板），双击或按回车才触发隐藏面板 + 自动粘贴<br><br>用户可在设置中切换交互模式。 | P0 |
| F-05 | 记录删除 | 支持单条删除与清空全部历史。清空全部历史时需弹出二次确认对话框。 | P0 |
| F-06 | 系统托盘驻留 | 程序启动后在系统托盘中运行。托盘右键菜单至少包含：显示剪切板面板、设置、关于、退出。支持托盘图标左键单击直接弹出面板。 | P0 |

### 2.2 增强功能（P1 - 建议实现）

| 编号 | 功能 | 描述 | 优先级 |
|------|------|------|--------|
| F-07 | 记录类型识别 | 区分文本、图片、文件路径等类型，面板中显示类型图标。类型检测规则：① 文本内容以盘符路径（如 `C:\`、`D:\`）或 UNC 路径（`\\`）开头 → 调用 `Path::exists()` 或 `GetFileAttributesW` 验证路径是否真实存在，仅当路径实际存在时才标记为"文件路径"，否则按普通文本处理；② 图片/位图数据 → 标记为"图片"；③ 其余纯文本 → 标记为"文本"。若剪切板同时包含多种格式，按"文本 > 图片 > 文件路径"优先级显示内容。图标集参考 Ditto 等成熟工具的标准集。<br><br>**存储字段约定**: `content_type = 'file'` 时，文件路径字符串存入 `content_text` 字段；`content_type = 'image'` 时，图片二进制数据存入 `content_blob` 字段。 | P1 |
| F-08 | 收藏/置顶 | 将常用记录标记为收藏，置于列表顶部。收藏状态持久化保存。 | P1 |
| F-09 | 搜索过滤 | 在面板内输入关键词实时过滤历史记录。**搜索范围**: 联合搜索 `content_text`（文本内容）和 `source_app`（来源应用名称）。**匹配模式**: 采用模糊搜索（推荐 SQLite FTS5 全文索引），支持大小写不敏感匹配，不支持正则表达式（v1）。搜索性能需满足 NF-03 面板响应指标。<br><br>**非文本内容搜索**: 对于图片类型记录（`content_text` 为空），搜索不匹配任何关键词，这些记录不会出现在搜索结果中。文件路径类型记录（`content_type = 'file'`）可通过对 `content_text` 中的路径字符串进行 FTS5 搜索。FTS5 不索引 BLOB 列，因此图片内容不可被搜索，此行为在用户界面中应通过占位文字（如"搜索仅支持文本及文件路径内容"）向用户说明。 | P1 |
| F-10 | 记录数量限制 & 自动清理 | 用户可设置最大保留条数。**默认值为 5000 条**，超出后自动淘汰最旧记录（按 `created_at` 升序淘汰）。用户可设为 **-1**（设置界面显示为"无限制"）以禁用限制，此时所有记录永久保留（需用户自行管理存储空间）。<br><br>**性能说明**: 当记录数超过 10,000 条时，系统不会崩溃，但面板加载、搜索响应等操作性能会逐步下降（10,000 条以上不再受 NF-03 性能指标保证）。建议普通用户保持默认值 5000 条，仅高级用户在了解性能影响后选择无限制模式。<br><br>配置项位于设置窗口"存储管理"分组中。 | P1 |
| F-11 | 窗口焦点自动粘贴 | 按两步法实现：面板带焦点供浏览选择 → 选择记录后隐藏面板 → 恢复焦点到原活动窗口 → 模拟 `Ctrl+V` 粘贴。面板弹出时使用 `WS_EX_NOACTIVATE` 风格以避免夺取焦点（仅置顶显示），用户选择后才隐藏面板并恢复原窗口焦点。 | P1 |
| F-17 | 忽略应用列表 | 允许用户设置"忽略应用"列表，在被忽略的应用中的复制操作**不被**记录。配置方式支持：① 窗口标题匹配（支持通配符）；② 进程名称（exe 名称）匹配；③ 完整路径匹配。<br><br>**匹配策略**（按精确度从高到低，可短路）：进程名匹配 → 完整路径匹配 → 窗口标题匹配。若进程名已匹配成功，则无需继续检查窗口标题。<br><br>**性能考量**: 规则数量上限建议不超过 50 条；50 条规则下的单次匹配耗时应在 1ms 以内。规则更新后应刷新缓存（如进程名 → 完整路径的映射表），避免每次剪贴板变更都重新解析配置。<br><br>**命名说明**: 该功能原称"应用白名单"，因实际语义为"黑名单"（列表中的应用不记录），v3 已更名为"忽略应用列表"以避免语义歧义。<br><br>该功能是第 6 节隐私风险缓解措施的具体实现。 | P1 |
| F-18 | （已删除，内容合并至 NF-06） | — | — |

### 2.3 扩展功能（P2 - 可后续迭代）

| 编号 | 功能 | 描述 | 优先级 |
|------|------|------|--------|
| F-12 | 分类/分组 | 按来源应用、内容类型自动或手动分类 | P2 |
| F-13 | 云同步 | 跨设备同步剪切板历史（优先支持 WebDAV 或本地网络共享，避免引入账号系统） | P2 |
| F-14 | 富文本/图片预览 | 在面板中预览图片及富文本格式内容。图片预览支持两种来源：① 用户复制纯图片（仅图片格式）时的二进制数据；② 文本+图片多格式共存时，F-01 存储策略保留的图片备份数据。 | P2 |
| F-15 | 快捷键快速粘贴历史第 N 条 | 如 `Ctrl+Shift+1~9` 直接粘贴对应条目 | P2 |
| F-16 | 多语言支持 | 中文/英文界面切换 | P2 |

---

## 3. 非功能需求

| 编号 | 需求 | 指标 | 验证手段 |
|------|------|------|---------|
| NF-01 | 启动速度 | 程序启动到托盘驻留 ≤ 1 秒 | — |
| NF-02 | 内存占用 | 空闲状态下内存占用 ≤ 30 MB（纯文本场景）。图片功能开启后内存限制单独评估。<br><br>**变更说明**: v2 曾放宽至 50 MB，v3 回归 v1 的 30 MB 指标。Rust 语言 + SQLite 轻量栈下 30MB 是合理且可达的指标。 | — |
| NF-03 | 面板响应 | ① 热键唤起面板：从热键按下到面板首帧渲染完成 ≤200ms（不区分冷启动/热启动，均以首帧渲染为准）；② 列表滚动：纯文本列表 60fps，图片内容 ≥30fps；③ 10,000 条记录下首次从数据库加载完成 ≤500ms。<br><br>**说明**: 10,000 条为性能保证上限，用户默认配置为 5000 条（参见 F-10）。存储记录超过 10,000 条时性能可能下降，超出部分的加载时间不在 NF-03 保证范围内。 | 使用高精度计时器在 CI 中编写性能基准测试，每次构建自动比对基线值。 |
| NF-04 | 存储上限 | 存储上限 ≥ 10,000 条文本记录（受用户配置限制，默认上限 5,000 条，参见 F-10） | — |
| NF-05 | 兼容性 | 支持 Windows 10 1903+ 和 Windows 11 | — |
| NF-06 | 安全性 | ① 不收集用户隐私数据；② 除云同步功能外，程序**不建立任何网络连接**；③ 本地数据库采用明文 SQLite 存储（不加密），但需在首次启动时明确告知用户存储安全模型；④ 程序编译时通过代码审查确保无隐藏网络调用依赖，审查范围包括：所有依赖库的网络能力声明、项目代码中的网络 API 调用（如 `reqwest`、`hyper`、`winhttp` 等），审查结果记录在构建流程中。 | ① 通过抓包工具（Wireshark/Fiddler）验证无外发网络请求；② 代码审查记录纳入构建流程。 |
| NF-07 | 可靠性 | 7×24 小时稳定运行，不崩溃、不泄漏 | — |

---

## 4. 项目结构设计

### 4.1 技术选型建议

| 层级 | 技术方案 | 说明 |
|------|---------|------|
| 编程语言 | **Rust** 或 **C# (WPF)** | Rust 性能好、体积小；C# WPF 开发效率高、Windows 生态好 |
| UI 框架 | **Rust: egui / Tauri**；**C#: WPF / WinUI 3** | 面板 UI 需要支持全局弹出、半透明、焦点穿透等特性 |
| 剪切板监听 | **Rust: clipboard-win + win32 API**；**C#: System.Windows.Clipboard + Win32 API** | 需通过 Windows 消息循环监听 WM_CLIPBOARDUPDATE |
| 数据存储 | **SQLite**（通过 rusqlite 或 Microsoft.Data.Sqlite），搜索建议启用 FTS5 全文索引 | 轻量、零配置、嵌入式 |
| 全局热键 | **Rust: global-hotkey**；**C#: RegisterHotKey Win32 API** | 注册系统级快捷键 |
| 构建工具 | **Rust: cargo**；**C#: MSBuild / dotnet build** | - |

> **推荐方案**: Rust + egui + SQLite + tray-icon，打包为单个可执行文件，体积小、免安装。

### 4.2 目录结构

```
ClipBoardX/
├── README.md                        # 项目说明
├── LICENSE                          # 开源许可证
├── Cargo.toml                       # Rust 项目配置 (若选用 Rust)
│
├── docs/                            # 文档
│   ├── 需求_v4.md                   # 本文件
│   ├── 设计_架构.md                 # 架构设计文档
│   └── 设计_数据库.md               # 数据库表结构设计
│
├── assets/                          # 静态资源
│   ├── icon.ico                     # 托盘图标 / 应用图标
│   ├── icon.png                     # PNG 格式图标
│   └── fonts/                       # 自定义字体（可选）
│
├── src/                             # 源代码
│   ├── main.rs                      # 入口：初始化、启动托盘、注册热键
│   ├── app.rs                       # 应用状态管理
│   │
│   ├── clipboard/                   # 剪切板模块
│   │   ├── mod.rs
│   │   ├── listener.rs             # 系统剪切板监听（WM_CLIPBOARDUPDATE）
│   │   ├── recorder.rs             # 捕获内容并保存到数据库
│   │   └── types.rs                # 剪切板数据结构定义
│   │
│   ├── ui/                          # 用户界面模块
│   │   ├── mod.rs
│   │   ├── panel.rs                # 剪切板面板主窗口
│   │   ├── list_view.rs            # 历史记录列表组件
│   │   ├── search_bar.rs           # 搜索框组件
│   │   ├── settings_window.rs      # 设置窗口
│   │   └── theme.rs                # 主题 / 样式定义
│   │
│   ├── storage/                     # 数据持久化模块
│   │   ├── mod.rs
│   │   ├── database.rs             # SQLite 初始化 & 连接管理
│   │   ├── models.rs               # 数据模型 (ClipRecord)
│   │   └── repository.rs           # CRUD 操作
│   │
│   ├── hotkey/                      # 全局热键模块
│   │   ├── mod.rs
│   │   └── manager.rs              # 热键注册/注销、回调分发、冲突检测
│   │
│   └── utils/                       # 工具函数
│       ├── mod.rs
│       └── config.rs               # 配置管理 (JSON/TOML)
│
├── tests/                           # 集成测试
│   ├── clipboard_test.rs
│   ├── storage_test.rs
│   └── integration_test.rs
│
├── scripts/                         # 构建 & 部署脚本
│   ├── build.ps1                   # Windows 构建脚本 (PowerShell)
│   ├── install.ps1                 # 安装到开机启动
│   └── package.ps1                 # 打包为发布版本
│
└── .gitignore                       # Git 忽略规则
```

### 4.3 数据表结构（v3 修订版）

**表名**: `clip_records`

| 列名 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PRIMARY KEY AUTOINCREMENT | 自增主键 |
| content_hash | TEXT UNIQUE | 内容哈希值（SHA256 或 MurmurHash），用于去重（配合 UNIQUE 约束，防止短时间内相同内容重复存储） |
| content_type | TEXT | 内容类型：text / image / file |
| content_text | TEXT | 文本内容（text 类型时存储）；file 类型时存储文件路径字符串 |
| content_blob | BLOB | 二进制内容（image 等类型时存储）；多格式共存时，F-01 存储策略保留的图片备份也存入此字段 |
| source_app | TEXT | 来源应用名称（可选） |
| source | TEXT | 记录来源枚举值：`local_clipboard`（用户手动复制）/ `cloud_sync`（云同步）。**注意**: 程序自身写回剪切板的操作通过 F-01 的跳过机制（暂停/恢复或内存标记）阻止被重复记录，因此不需要 `app_writeback` 枚举值。 |
| is_favorite | INTEGER DEFAULT 0 | 是否收藏 (0/1) |
| created_at | DATETIME DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| updated_at | DATETIME DEFAULT CURRENT_TIMESTAMP | 更新时间 |

> **v2 → v3 变更**: `source` 字段删除 `app_writeback` 枚举值（写回跳过机制已在 F-01 中由运行时逻辑处理，无需在数据层面标记）；`content_text` 字段说明扩展为"text 类型时存储文本内容；file 类型时存储文件路径字符串"。

**全文搜索索引**（可选，建议 FTS5）：

```sql
CREATE VIRTUAL TABLE clip_records_fts USING fts5(
    content_text, source_app,
    content='clip_records',
    content_rowid='id'
);
```

> 启用 FTS5 全文索引可支持高效模糊搜索，满足 NF-03 响应指标。注意：FTS5 仅索引 `content_text` 和 `source_app` 两列，图片等非文本内容（BLOB 列）不参与搜索。

---

## 5. 交互流程

### 5.1 用户操作流程

```
用户操作                 系统响应
─────────               ─────────
1. 用户复制文本 (Ctrl+C)  → 系统剪切板更新
                          → 监听器捕获变更
                          → 检查应用忽略列表（F-17）
                          → 检查 content_hash 是否已存在（去重）
                          → 若为新内容（多格式共存按 F-01 策略保留纯文本+图片），写入 SQLite（source = local_clipboard）
                          → 更新面板列表（若有缓存）

2. 用户按下全局热键       → 检测热键是否被占用（首次运行时）
                          → 剪切板面板弹出（WS_EX_NOACTIVATE，不夺取焦点）
                          → 从数据库加载历史记录（按时间倒序）
                          → 渲染列表，首帧完成时间 ≤200ms

3. 用户浏览/搜索          → 实时过滤列表
                          （联合搜索 content_text + source_app，FTS5 模糊匹配）
                          非文本内容不参与搜索，界面提示"搜索仅支持文本及文件路径内容"

4a. 用户单击某条记录      → [自动粘贴模式 - 默认]
   （自动粘贴模式）         → 写回跳过机制（参见 F-01）
                          → 将该记录写入系统剪切板
                          → 隐藏面板
                          → 恢复焦点到原活动窗口
                          → 模拟 Ctrl+V 自动粘贴

4a'. 用户单击某条记录     → [确认后粘贴模式]
   （确认后粘贴模式）       → 将该记录写入系统剪切板（不隐藏面板，不自动粘贴）
                          → 面板保持显示，等待用户双击或按回车

4b. 用户双击/按回车       → [确认后粘贴模式]
                          → 写回跳过机制（参见 F-01）
                          → 将该记录写入系统剪切板
                          → 隐藏面板
                          → 恢复焦点到原活动窗口
                          → 模拟 Ctrl+V 自动粘贴

4c. 用户删除某条记录      → 从数据库删除
                          → 列表实时更新

5. 用户再次按热键/ESC    → 面板隐藏
```

### 5.2 系统生命周期

```
应用程序启动
    │
    ▼
读取配置（热键、主题、最大条数、忽略应用列表等）
    │
    ▼
初始化 SQLite 数据库（建表/迁移）
    │
    ▼
检测默认热键是否被占用 → 若冲突则提示用户重设
    │
    ▼
注册全局热键 & 系统托盘图标
    │
    ▼
启动剪切板监听循环（窗口消息循环）
    │
    ▼
┌─────────────────────────────────────────┐
│  等待事件：                              │
│  ├─ 剪切板变更 → 检查来源跳过标记       │
│  │             → 检查应用忽略列表       │
│  │             → content_hash 去重      │
│  │             → 保存记录               │
│  ├─ 热键触发   → 显示/隐藏面板          │
│  ├─ 托盘菜单   → 面板/设置/关于/退出    │
│  └─ 退出信号   → 资源清理 → 退出        │
└─────────────────────────────────────────┘
```

---

## 6. 风险与注意事项

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| Windows 剪切板 API 限制 | 某些应用使用自定义剪切板格式，可能无法捕获 | 回退到定时轮询 + 支持多种格式 |
| 全局热键冲突 | 与其他软件热键冲突导致功能失效 | 提供可自定义热键 + 首次启动时热键冲突检测 |
| 写入回触发监听循环 | 写回剪切板触发监听器，导致重复数据 | 使用暂停/恢复机制或内存标记跳过自触发（参见 F-01 写回跳过机制）；使用内存标记时需注意异常安全，确保标记在 finally/Defer 块中被正确清除 |
| 面板焦点管理 | 面板夺取焦点后无法自动粘贴到目标窗口 | 采用两步法：面板不夺取焦点（`WS_EX_NOACTIVATE`）→ 选择后隐藏面板 → 恢复焦点 → 模拟 Ctrl+V（参见 F-11） |
| 隐私顾虑 | 用户不希望敏感内容被记录 | 提供"忽略应用列表"功能（F-17）+ 一键清空 + 本地存储不上传 + 首次运行告知存储安全模型 |
| 高频率复制导致性能问题 | 连续快速复制触发大量数据库写入 | 防抖去重（content_hash UNIQUE 约束）+ 异步写入 |

---

## 7. 附录

### 7.1 参考产品

- **Ditto Clipboard Manager**（Windows 开源剪切板管理器）
- **1Clipboard**（Mac/Windows 剪切板管理器）
- 搜狗输入法 / 百度输入法 / 讯飞输入法 的剪切板功能

### 7.2 相关 Win32 API

| API | 用途 |
|-----|------|
| `AddClipboardFormatListener` | 注册剪切板变更监听 |
| `RemoveClipboardFormatListener` | 注销监听 |
| `GetClipboardData` | 获取剪切板内容 |
| `SetClipboardData` | 设置剪切板内容 |
| `RegisterHotKey` | 注册全局热键 |
| `UnregisterHotKey` | 注销全局热键 |
| `CreateWindowEx(WS_EX_TOOLWINDOW / WS_EX_NOACTIVATE...)` | 创建全局顶层窗口（不夺取焦点） |
| `Shell_NotifyIcon` | 系统托盘图标 |

### 7.3 版本间变更摘要（v3 → v4）

| 编号 | 变更项 | 类型 | 对应审议 |
|------|--------|:----:|:--------:|
| 1 | **F-01 存储策略调整**：将"多格式共存时只存最高优先级格式"改为"保留最高优先级的两种格式（纯文本 + 图片）"，确保 `content_blob` 在多格式场景下也有数据可用，解决 F-07 图片识别和 F-14 图片预览的死逻辑问题。同时在写回跳过机制中补充异常安全要求（标记位必须在 finally/Defer 中清除） | 🔴 修复 | S-01 |
| 2 | **F-02 回归 v1 简洁描述**：移除"数量限制相关配置参见 F-10"的冗余引用，F-02 仅描述持久化存储职责 | 🔴 修复 | S-02 |
| 3 | **NF-03/NF-04 术语统一**：NF-03 中"10,000 条为设计上限指标"改为"10,000 条为性能保证上限"；NF-04 中"支持至少 10,000 条"改为"存储上限 ≥ 10,000 条"；F-10 补充禁用限制场景下的性能说明（10,000 条以上系统不崩溃但性能逐步下降） | 🔴 修复 | S-04 |
| 4 | **删除 F-18，合并入 NF-06**：F-18 安全性验证内容完整并入 NF-06 第④点，消除重复定义 | 🔴 修复 | S-05 |
| 5 | **F-10 禁用限制值改为 -1**：原"设为 0 禁用限制"违反用户直觉（0 通常表示不保存），改为 -1 并在设置界面显示为"无限制"；同时补充性能预期说明 | 🟡 修复 | M-01 |
| 6 | **F-07 文件路径检测增加路径存在性验证**：对匹配盘符/UNC 模式的字符串，调用 `Path::exists()` 或 `GetFileAttributesW` 验证路径真实存在后才标记为"文件路径"，否则按普通文本处理 | 🟡 修复 | M-02 |
| 7 | **5.1 流程图简化**：4a/4b 中监听器三段式描述简化为"写回跳过机制（参见 F-01）"，避免与 F-01 重复 | 🟡 修复 | M-04 |
| 8 | **F-17 补充匹配策略与性能考量**：定义匹配优先级（进程名→完整路径→窗口标题，可短路）、规则上限（建议 ≤50 条）、缓存刷新策略 | 🟡 修复 | M-05 |
| 9 | **F-04 交互模式重命名**："单击模式/双击模式"改为"自动粘贴模式/确认后粘贴模式"，消除命名与实际行为不符的混淆 | 🟡 修复 | M-06 |
| 10 | **1.2 目标更新**：同步 F-10 的"可关闭限制"→"可设为无限制" | — | 伴随修复 |
| 11 | **F-14 图片预览说明补充**：明确图片预览支持两种来源（纯图片复制、文本+图片共存时保留的备份） | — | 伴随修复 |
| 12 | **6. 风险表更新**：监听循环缓解措施增加异常安全说明 | — | 伴随修复 |

---

> **文档版本记录**
>
> | 版本 | 日期 | 修改内容 | 作者 |
> |------|------|---------|------|
> | v1.0 | 2025-01 | 初始版本，完成核心需求描述与项目结构设计 | AI |
> | v2.0 | 2025-01 | 根据审议_v1.1 修订：修复 2 项严重问题（写入回环、焦点管理）、9 项一般问题 | AI |
> | v3.0 | 2025-01 | 根据审议_v2.1 修订：修复 5 项严重问题（NF-02 指标弱化、F-02/F-10 重复、F-04 矛盾、NF-06 审计日志、app_writeback 死逻辑）与 5 项一般问题（F-17 命名、NF-03 说明、存储字段、非文本搜索、监听器生命周期） | AI |
> | v4.0 | 2025-01 | 根据审议_v3.1 修订：修复 4 项严重问题（S-01 格式优先级矛盾、S-02 职责分离不彻底、S-04 指标矛盾、S-05 重复定义）与 5 项一般问题（M-01 UX 设计缺陷、M-02 路径误判、M-04 冗余描述、M-05 性能缺失、M-06 命名混淆） | AI |
